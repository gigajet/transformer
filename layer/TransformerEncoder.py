import torch
from torch import nn

class TransformerEncoder (nn.Module):
    def __init__(self, d_model: int, d_ff: int, n_head: int, ) -> None:
        super().__init__()

    def forward(self, ...):
        pass